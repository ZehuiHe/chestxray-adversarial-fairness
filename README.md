# Adversarial Unlearning for Sex-Based Subgroup Disparities in Chest X-Ray Classification

Code and results for the MSc dissertation *Evaluating Adversarial Unlearning for
Sex-Based Subgroup Performance Disparities in Chest X-Ray Classification*.

- **Author:** Zehui He
- **Degree:** MSc Advanced Computer Science (Data Analytics), University of Leeds, 2025/2026
- **Supervisor:** Dr Toni Lassila

## What this project does

A chest X-ray classifier can look accurate overall while missing more disease in
one group of patients than another. This project asks whether *adversarial
representation learning* — training the network so that its internal features
become less useful for predicting a patient's recorded sex — reduces that
imbalance without damaging disease-prediction performance.

Four ResNet-50 variants are compared on seven NIH ChestX-ray14 pathologies under
an identical data split, disease objective, seeds and evaluation protocol:

| Variant | Description |
|---|---|
| Baseline | No adversary |
| Static GRL | Gradient reversal layer at fixed strength, lambda = 0.1 |
| Standard Dynamic GRL | Same maximum strength, DANN-style logistic schedule |
| Final Dynamic | Equalized-odds-conditioned adversaries, cell balancing, ramped conflict projection, gradient-norm balancing |

Headline result: against the baseline, Final Dynamic improves macro AUROC
(+0.0022), macro AUPRC (+0.0042) and worst-group recall (+0.0193), all surviving
Holm-Bonferroni correction in a paired 10,000-replicate patient-cluster
bootstrap. **No** recorded-sex gap reduction survives multiplicity correction.
The supported claim is a higher absolute performance floor, not a
correction-robust narrowing of the between-group gap.

## Repository structure

Matches Appendix A.3 of the dissertation.

| File | Content |
|---|---|
| `01_dataset_audit_fixed.ipynb` | Dataset acquisition, integrity audit, patient-level partitioning |
| `02_multilabel_feasibility_audit_Core.ipynb` | Two-stage label feasibility audit, multi-label target construction |
| `03_baseline.ipynb` | Baseline ResNet-50, three seeds |
| `04_static_adversarial_unlearning.ipynb` | Static GRL, three seeds |
| `05_dynamic_grl_scheduling.ipynb` | Standard Dynamic GRL, three seeds |
| `05b_dynamic_grl_fast_ramp_v2.ipynb` | Supplementary: fast cosine ramp |
| `05c_enhanced_dynamic_EO_projected.ipynb` | Supplementary: earlier EO-conditioned projected variant |
| `05d_utility_preserving_dynamic_EO_gradnorm.ipynb` | Final Dynamic |
| `06_final_results_analysis.ipynb` | Four-model aggregation, 10,000-replicate bootstrap, empirical p-values, Holm/BH correction, result figures |
| `r50_multilabel_final_common.py` | Shared data loading, ResNet-50/GRL components, threshold selection, subgroup metrics, bootstrap utilities |
| `r50_multilabel_final_common_v2.py` | Fast-ramp schedule fork, used only by `05b` |
| `results/` | Run configurations, training histories, thresholds, per-seed and aggregated metrics, bootstrap distributions and confidence intervals, multiplicity tables |
| `figures/` | Experiment-generated schedule and result plots |

## Reproducing the results

**1. Data.** Obtain NIH ChestX-ray14 (112,120 images, 30,805 patients). The
notebooks fetch it with `kagglehub` under the identifier `nih-chest-xrays/data`.
The images and `Data_Entry_2017.csv` are not redistributed here.

**2. Environment.**

```
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```

Developed on Python 3.10 with PyTorch and an Apple Silicon GPU (`mps`). Any
CUDA device works; set the device in the shared module.

**3. Run order.** Notebooks are numbered in dependency order. `01` and `02` must
run first — they create the fixed split manifests every later notebook loads.
`05d` requires a completed baseline checkpoint from `03`. `06` aggregates the
four main variants and produces every number in Chapter 4.

## What is deliberately not included

- **Trained checkpoints** (~2.2 GB). Available on request; every reported metric
  is recomputable from the stored prediction archives without them.
- **Split manifests and per-image prediction archives.** These carry Patient ID
  and recorded sex for individual radiographs. Appendix B of the dissertation
  undertakes not to redistribute patient-level metadata, so they are omitted
  here. The splits are fully deterministic: `01_dataset_audit_fixed.ipynb`
  regenerates them exactly using `GroupShuffleSplit` grouped on Patient ID with
  seed 42.
- **Per-step lambda and gradient traces** (~90 MB of diagnostics). Regenerated by
  re-running the corresponding notebook.

## Notes

- Every reported number derives from a fixed patient-level split; no patient
  appears in more than one partition.
- Per-label decision thresholds are selected on validation data only and frozen
  before test evaluation, then applied identically to both recorded-sex groups.
- The protected attribute is the binary female/male field recorded in the NIH
  metadata. It is used only as a subgroup attribute for training and evaluation,
  is never an input to the disease model, and is not treated as a representation
  of gender identity.
- Model outputs are not clinical diagnoses. Nothing here is validated for
  clinical use.

## Licence

Code released under the MIT Licence. The NIH ChestX-ray14 dataset is subject to
its own terms and is not redistributed.
